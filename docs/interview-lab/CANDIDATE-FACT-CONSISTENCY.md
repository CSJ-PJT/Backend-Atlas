# Candidate Fact Consistency

공개 안전 fact 6개와 candidate-scope 문항 120개의 evidenceFactIds를 대조했다. 미해석 fact ID는 0개다.

## 강제 경계

- 실제 소속회사와 프로젝트 투입처를 혼동하지 않는다.
- SAP ERP REST API 연동만 말한다.
- Archive/Atlas는 개인 프로젝트이며 합성·비식별 데이터 중심이다.
- AI 도구와 본인의 요구사항·계약·검증 책임을 구분한다.
- 자체 LLM 학습, GPU 모델 서버, 직접 DSP 실무, Staff 인사관리 경험을 만들지 않는다.
- 약 855배는 처리 건수와 실행 시간을 분당 처리량으로 정규화한 비교다.

`test-interview-fact-consistency.mjs` 결과: PASS. 최신 최종 3개 후보자 문서가 없으므로 소속 관계와 지원 완료 여부는 계속 `needs-confirmation`이다.
